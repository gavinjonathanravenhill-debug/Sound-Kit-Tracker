{
  "name": "sound-kit-tracker",
  "version": "1.0.0",
  "description": "QR-based inventory tracker for broadcast sound kit",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
